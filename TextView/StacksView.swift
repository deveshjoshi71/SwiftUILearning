//
//  StacksView.swift
//  TextView
//
//  Created by Infoicon on 18/04/24.
//

import SwiftUI

struct StacksView: View {
    var body: some View {
        ScrollView {
            VStack(spacing: 0) {
                HStack(spacing: 0) {
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                }
                HStack(spacing: 0) {
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                }
                HStack(spacing: 0) {
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                }
                HStack(spacing: 0) {
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                }
                HStack(spacing: 0) {
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                }
                HStack(spacing: 0) {
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                }
                HStack(spacing: 0) {
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                }
                HStack(spacing: 0) {
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.white)
                        .frame(height: 50)
                    Rectangle()
                        .fill(Color.black)
                        .frame(height: 50)
                }
            }
            .border(Color.black, width: 4)
            .padding()
        }
        .scrollIndicators(.hidden)
    }
}

struct HStacksView: View { //horizontal
    var body: some View {
//        VStack(alignment: .leading, spacing: 10, content: {
////            Rectangle()
////                .fill(Color.purple)
////                .frame(width: 50, height: 50)
//            Text("5")
//                .font(.largeTitle)
//            Text("Number of item")
//                .font(.body)
//            Text("Hey there this is your cart.")
//                .font(.caption)
//        })
        VStack(spacing: 50) {
            
            ZStack {
                Circle()
                    .frame(width: 150, height: 100)
                
                Text("1")
                    .font(.largeTitle)
                    .fontWeight(.semibold)
                    .foregroundStyle(Color(.white))
            }
            
            Text("1")
                .font(.largeTitle)
                .fontWeight(.semibold)
                .foregroundStyle(Color(.white))
                .background(
                    Circle()
                        .frame(width: 150, height: 100)
                )
            
        }
    }
}

struct VStacksView: View { //vertical
    var body: some View {
        VStack(alignment: .leading, spacing: 10, content: {
            Rectangle()
                .fill(Color.orange)
                .frame(width: 150, height: 150)
            Rectangle()
                .fill(Color.pink)
                .frame(width: 100, height: 100)
            Rectangle()
                .fill(Color.cyan)
                .frame(width: 50, height: 50)
            
        })
        
    }
}

struct ZStacksView: View { // over each other
    var body: some View {
        ZStack(alignment: .top) {
            Rectangle()
                .fill(Color.orange)
                .frame(width: 350, height: 500, alignment: .center)
            VStack(alignment: .leading, spacing: 15) {
                Rectangle()
                    .fill(Color.yellow)
                    .frame(width: 150, height: 150)
                
                Rectangle()
                    .fill(Color.purple)
                    .frame(width: 100, height: 100)
                
                HStack(alignment: .bottom, spacing: 10) {
                    Rectangle()
                        .fill(Color.green)
                    .frame(width: 50, height: 50)
                    Rectangle()
                        .fill(Color.red)
                    .frame(width: 50, height: 50)
                    Rectangle()
                        .fill(Color.green)
                    .frame(width: 50, height: 50)
                    Rectangle()
                        .fill(Color.red)
                    .frame(width: 50, height: 50)
                    
                }
                .background(Color.white)
            }
            .background(Color.black)
        }
    }
}




#Preview {
    //StacksView()
    HStacksView()
    //ZStacksView()
    //VStacksView()
}
